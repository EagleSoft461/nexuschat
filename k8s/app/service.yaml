apiVersion: v1
kind: Service
metadata:
  name: nexuschat-service
  namespace: nexuschat
  labels:
    app: nexuschat-app
spec:
  selector:
    app: nexuschat-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
