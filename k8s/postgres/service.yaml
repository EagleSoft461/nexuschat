apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: nexuschat
  labels:
    app: postgres
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP   # Internal only — not exposed outside the cluster
