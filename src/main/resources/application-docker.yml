spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/nexuschat
    username: nexuschat
    password: nexuschat

  data:
    redis:
      host: redis
      port: 6379

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

server:
  port: 8080

logging:
  level:
    com.nexuschat: INFO
